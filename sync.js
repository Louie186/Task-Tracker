/* sync.js — placeholder.
   Lew's Tracker runs fully local (data lives in this browser's storage).
   This file exists so the page loads clean with no 404 errors.
   If cloud sync (Firebase/Supabase) is added later, the code goes here. */
